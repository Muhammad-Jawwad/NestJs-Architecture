export enum AuthType {
    Google = "google",
    Jwt = "jwt",
    Facebook = 'facebook',
    Amazon = 'amazon',
}

export enum Roles {
    Admin = "admin",
    User = "user"
}

export enum PlanType {
    Bronze = "bronze",
    Silver = "silver",
    Gold = "gold",
    Platinum = "platinum",
}

export enum Currency {
    USD = "usd"
}

export enum PaymentStatus {
    Unpaid = "unpaid",
    Paid = "paid",
    Declined = "declined"
}
  